[project]
name = "chunker"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pymupdf>=1.26.6",
    "pywebview>=6.1",
    "scikit-learn>=1.7.2",
    "sentence-transformers>=5.1.2",
]
